import React from 'react';
import { SkillRateBox } from 'index';
import { decorator } from '../../utils/decorator';

export default decorator('SkillRateBox').add('SkillRateBox Component', () => (
  <>
    <SkillRateBox />
  </>
));
